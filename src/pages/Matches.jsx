/**
 * Matches page (route placeholder).
 *
 * Intended to host the match listings (live, upcoming, recent) built
 * on top of match.service. No implementation yet; this module is a
 * placeholder so the route and file structure exist for future work.
 */
